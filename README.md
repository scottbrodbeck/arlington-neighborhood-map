# Arlington Neighborhood Map (ARLnow canon)

## Web app

`docs/` is a static neighborhood-lookup web app (GitHub Pages serves it from
this folder): a MapLibre GL map with the ARLnow neighborhoods overlaid, plus
search for an address (`3100 Columbia Pike`) or block (`2000 block of
N Quincy St`). Results are border-aware — an address within ~80 m of another
neighborhood reports "on the border of X and Y." Clicking anywhere on the map
gives the same readout.

- Basemap: OpenFreeMap `positron` (keyless, free). Geocoding: Arlington
  County GIS (`arlgis.arlingtonva.us`, authoritative) with the U.S. Census
  geocoder as a JSONP fallback. No API keys, no backend.
- Local dev: `.venv/bin/python scripts/dev_server.py` → http://127.0.0.1:8741
- Data files (`docs/neighborhoods.json`, `docs/labels.json`,
  `docs/county.json`) are generated by `scripts/build_neighborhoods.py` —
  regenerate after any boundary change.

`arlington_neighborhoods.geojson` is ARLnow's canonical neighborhood boundary
file: Arlington County's civic association polygons with the newsroom's
hand-annotated wall-map changes applied. **67 features**, same property schema
as the county file (`CIVIC` / `LABEL` hold the neighborhood name).

## How it was made

```
.venv/bin/python scripts/build_neighborhoods.py
```

Inputs (all checked in under `data/`):

- `data/Civic_Association_Polygons.geojson` — Arlington County civic
  association polygons (already includes the Green Valley, Arlington Mill,
  Shirlington and Crystal City updates that were hand-drawn on the older
  printed map).
- `data/osm/` — OpenStreetMap extracts (street centerlines, the county
  boundary, and the DCA airport polygon) used for split lines and gap fills.

Verification renders are in `maps/` (`overview_after.png` plus close-ups of
each changed area).

## Changes applied from the annotated wall map

| Annotation on map | Change |
|---|---|
| "Arlington" struck from "Arlington - East Falls Church" | Renamed **East Falls Church** |
| "Hall's Hill" written above "John M. Langston" | Renamed **Hall's Hill / John M. Langston** |
| "North" struck from "North Rosslyn" + loop drawn around the high-rise district | **Rosslyn** = North Rosslyn + the part of Radnor/Ft.Myer Heights north of **14th St N**. The bluff south of 14th St N remains **Radnor/Ft.Myer Heights** (the printed label sat outside the drawn loop) |
| Outline around Ballston with arrows to "Ballston" and "Virginia Square" | Split along **N. Quincy St** → **Ballston** (west) / **Virginia Square** (east) |
| "Clarendon / Courthouse" arrows + box around the courthouse area | Split along **N. Barton St** → **Clarendon** (west) / **Courthouse** (east) |
| "Pentagon City" box | Carved out of Aurora Highlands: I-395 (N), S. Joyce St (W), S. Eads St / Crystal City boundary (E); south edge runs **15th St S → S. Hayes St → 18th St S**, matching the notch drawn around the fire station / Aurora Hills library block |
| "Country Club Hills" label on unassigned area | New polygon filling the hole between Old Glebe, Gulf Branch, Bellevue Forest, Donaldson Run, Old Dominion and Rock Spring (Washington Golf & Country Club area) |
| "Potomac Yard or National Landing" label | New polygon **Potomac Yard (National Landing)** filling the unassigned strip south of Crystal City between US-1/Aurora Highlands, the DCA airport fence and the Alexandria county line, plus the Crystal Dr / Potomac Ave hotel block north of 33rd St S |
| (round 2) Joint Base circled | New polygon **Joint Base Myer-Henderson Hall** — the OSM base boundary intersected with unassigned land; Arlington National Cemetery stays unassigned |
| (round 2) empty spots circled | Gap fills merged into neighbors: Brandon Village block → **Buckingham**; Memorial Baptist Church block + the 36th/Glebe triangle (one pinched gap) → **Old Glebe**; the block south of S. George Mason Dr → **Claremont**; the unassigned pocket east of Shirlington, up to but not across I-395 → **Shirlington** |
| (round 3) | **Cherry Valley Nature Area** dropped as its own feature and merged into **Cherrydale** |
| (round 3) | **Potomac Yard** clipped to stay west of the **GW Parkway** (drops the airport W Entrance block and the shoreline strip south of the airfield; the Potomac Ave hotel block stays) |

Annotations that needed **no change** (already reflected in the county data):

- ~~Nauck~~ → **Green Valley** (county renamed it in 2020)
- ~~Columbia Heights West~~ → **Arlington Mill**
- **Shirlington** (drawn onto the old map; the county file already has it)
- **Crystal City** boundary tracing (already its own polygon)
- **Four Mile Run** — labels the stream itself, which already forms the
  boundary between the adjacent neighborhoods; no polygon change

## Judgment calls worth a second look

The marker lines are roughly a block wide at map scale, so the exact split
streets are interpretations:

1. **Clarendon/Courthouse divider = N. Barton St.** The drawn line passes
   between N. Barton and N. Adams; Barton was chosen because it spans the full
   polygon from Key Blvd to 10th St N (Adams stops at Clarendon Blvd).
2. **Rosslyn's southern edge = 14th St N.** The drawn line runs between
   14th Rd N and Fairfax Dr.
3. **Pentagon City's southern notch** keeps the Aurora Hills
   library/community-center block in Aurora Highlands, per the drawing.
4. **Potomac Yard** includes the US-1 right-of-way sliver down to Four Mile
   Run. The OSM airport polygon over-extends north of the real airfield (it
   covers the Capital View hotel block), so the airport is only subtracted
   south of the 33rd St S line.
5. Still intentionally unassigned: the airport, the Pentagon, Arlington
   National Cemetery, the GW Parkway shoreline, Army Navy Country Club,
   and island parks like Lubber Run — no street addresses there.

## Quick lookup example

```python
import json
from shapely.geometry import shape, Point

feats = json.load(open('arlington_neighborhoods.geojson'))['features']
pt = Point(-77.0599, 38.8627)            # lon, lat from any geocoder
print(next(f['properties']['CIVIC'] for f in feats
           if shape(f['geometry']).contains(pt)))   # -> Pentagon City
```
